import { ConfigReader, createConfigFile } from "remotion";

createConfigFile({
  entries: {
    "v01-chaos-to-order-social": "social/ChaosToOrder",
    "v02-dashboard": "video/v02-dashboard",
  },
});
